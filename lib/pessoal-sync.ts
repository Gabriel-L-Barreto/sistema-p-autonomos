/**
 * Stub do sync com planilha de Pessoal.
 * O módulo Pessoal não foi portado neste clone — o Controle funciona sem ele.
 */

export async function totaisPessoalPlanilhaPorObra(
  _db: unknown,
  _orcamentoId: number
): Promise<{ data: string; total: number; nomes: string[] }[]> {
  return [];
}
